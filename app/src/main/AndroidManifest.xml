<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.e_pharmacy">

    <application
        android:allowBackup="true"
        android:icon="@drawable/eph"
        android:label="@string/app_name"
        android:roundIcon="@drawable/eph"
        android:supportsRtl="true"
        android:theme="@style/Theme.Epharmacy">
        <activity android:name=".Jview"></activity>
        <activity android:name=".Jinsert"></activity>

        <activity android:name=".Jhomepage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>